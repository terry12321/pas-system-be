let rb = require('./rb');
let qp = require('@flexsolver/flexqp2');
let dev = { host: `dev.flex-solver.app`, user: 'qp_test', password: 'P@ssw0rd', database: `flexpos3`, limit: 2 };
qp.presetConnection(dev);
rb.setQpDriver(qp);

async function todo() {
    let list = await qp.select(`select * from settlements`);
    console.log(rb.renderTable(list));
}
// console.log(rb.build({}))

todo();